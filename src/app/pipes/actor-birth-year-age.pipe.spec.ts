import { ActorBirthYearAgePipe } from './actor-birth-year-age.pipe';

describe('ActorBirthYearAgePipe', () => {
  it('create an instance', () => {
    const pipe = new ActorBirthYearAgePipe();
    expect(pipe).toBeTruthy();
  });
});
